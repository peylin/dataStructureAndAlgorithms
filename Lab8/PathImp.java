import CITS2200.*;
/**
 * 1. Apply Prim's algorithm to find a minimum spanning tree of an undirected, weighted graph.
 * 2. Apply Dijkstra's algorithm to find the shortest paths from a source vertex in a directed, weighted graph.
 * 
 * @author Peylin Ng
 * @version 09/05/18
 */
public class PathImp
{
    
    /**
     * Constructor for objects of class PathImp
     */
    public PathImp()
    {
        // initialise instance variables
        x = 0;
    }

    /**
     * Finds the minimum weight of a spoanning tree for the given graph. Prim's algorithm.
     * 
     * @param  g   the Graph to be searched. Assume that an edge weight of 0 signifies that 
     *             the edge does not exist.
     * @return     the weight of the minimum spanning tree, or -1 if there is no spanning tree
     */
    public int getMinSpanningTree(Graph g)
    {
        // put your code here
        return x + y;
    }
    
    /**
     * Runs Dijkstra's algorithm on a given undirected, non-negative weighted graph 
     * to find the distances to all vertices from the specified source vertex.
     * 
     * @param  g         the Graph to be searched. Assume that an edge weight of 0 signifies 
     *                   that the edge does not exist.
     *         source    the vertex on which to start the search
     * @return     an array listing the distance to each vertex in the single source shortest 
     *             path problem, or -1 if the vertex is not reachable from the source. 
     */
    public int[] getShortestPaths(Graph g, int source)
    {
        // put your code here
        return x + y;
    }
}
